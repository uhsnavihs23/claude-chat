// ─────────────────────────────────────────────────────────
// config.example.js  ←  Copy this to config.js and fill in
// config.js is in .gitignore — your key is NEVER committed
// ─────────────────────────────────────────────────────────
window.APP_CONFIG = {
  // Paste your OpenRouter key here for local dev only
  // Leave empty ("") to enter it via the sidebar at runtime
  OPENROUTER_API_KEY: "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",

  // Optional: lock to specific models (comment out to show all)
  // DEFAULT_MODEL: "nvidia/nemotron-3-super-120b:free",
};
